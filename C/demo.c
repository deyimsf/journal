#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX_COLS 20
#define MAX_INPUT 1000

/*
 枚举 enum Jar_Type{CUP,PINT,QUART,HALF_GALLON,GALLON}; 
 */

int main(){
	/*字符串*/
	while( gets(input) != NULL){
		printf("字符串: %s\n",input);
	}
}
